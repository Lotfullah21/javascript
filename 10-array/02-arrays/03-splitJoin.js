const subjects = ["AI", "PY", "ML", "DL", "ALGO"];

// string to array
const data = "Kabul, Herat, Baghlan, Paktia";
const newArr = data.split(",");
console.log("Converted array: ", newArr);

// array to string
const newString = newArr.join(",");
console.log("Converted string: ", newString);
