console.log("cat"[0]); // Give me the element at 0th index of "cat" string.
console.log("cat".charAt(21)); // Gives empty string
console.log("cat".charAt(2));

const message = "Hello people";
console.log(message.charAt(message.length - 1));

// Indexof()

const name = "Ahmad";
console.log(name.indexOf("a"));

// trim(): removes the empty spaces from beginning and end of a string
console.log(message.trim());
