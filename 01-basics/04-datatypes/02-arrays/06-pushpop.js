const subjects = ["AI", "PY", "ML", "DL", "ALGO"];

// push add elements to the end of an array.
subjects.push("Tensorflow");
console.log("using push to add tf", subjects);

// Remove from the end of an array.
subjects.pop();
console.log("After pop operation", subjects);
